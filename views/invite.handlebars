<html>
	{{> header}}
	<body id="{{page}}">
		<div id="banner">
			<img src="/www/img/grouplanner_logo.png">
			<h1>Welcome to GrouPlanner</h1>
		</div>

		<section>
			<p>Hey!</p>
			<p>You were invited to join this group on Grouplanner.</p>
		</section>
		
		<section>
			<div id="info">
				<i class="icon-pencil"></i>
				<h2>{{group.name}}</h2>
				<p>{{group.description}}</p>
				<div id="users">
					{{#each group.members}}
					<div class="user">
						<div class="img"><img src="{{picture}}" /></div>
						<div class="name">{{name.first}}</div>
					</div>
					{{/each}}
				</div>
			</div>
		</section>
		
		<section>
			<h2>Please sign in to join</h2>
			<div id="loginBtns">
				<p class="loginBtn" id="google">
					<a class="btn" href="/auth/google">Sign in with Google</a>
				</p>
				<p class="loginBtn" id="facebook">
					<a class="btn" href="/auth/facebook">Sign in with Facebook</a>
				</p>
				<p class="loginBtn" id="twitter">
					<a class="btn" href="/auth/twitter">Sign in with Twitter</a>
				</p>
			</div>
		</section>
	</body>
	<script>{{{JShare}}}</script>
	<script src="/socket.io/socket.io.js"></script>
	<script src="/www/lib/jquery-2.1.1.min.js"></script>
	<script src="/www/lib/moment-with-locales.js"></script>
	<script src="/www/js/{{page}}.js"></script>
</html>
